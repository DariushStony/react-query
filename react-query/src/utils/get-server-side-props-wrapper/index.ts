interface IGetServerSidePropsWrapperProps {
  ssrMethos: Array<any>;
}

const getServerSidePropsWrapper = ({}: IGetServerSidePropsWrapperProps) => {
  // DOES STUFF

  if (true) {
    return undefined;
  }

  return { props: undefined };
};

export default getServerSidePropsWrapper;
